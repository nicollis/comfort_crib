%nav.navbar.navbar-static-top
  .container-fluid
    .navbar-header
      %a.navbar-brand{href: root_path}
        ComfortCrib
    %ul.nav.navbar-nav.navbar-right
      %li
        = link_to 'Become a Host', root_path
      %li
        = link_to 'Help', root_path
      - unless signed_in?
        %li
          = link_to 'Sign up', new_guest_registration_path
        %li 
          = link_to 'Log in', new_guest_session_path
      - else 
        %li
          = link_to 'My Account', edit_guest_profile_path(current_guest)
        %li 
          = link_to 'Log out', destroy_guest_session_path, method: :delete